version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    working_dir: /app
    command: ["./venv/Scripts/activate", "&&", "uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]